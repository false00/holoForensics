# Collection Index

This index tracks the collection contracts known to the current Rust runtime.

The desktop UI also presents a broader Windows forensic collection portfolio for demo and planning use. That UI surface intentionally includes additional evidence sources beyond the active runtime contracts below. Unless a collection appears in this index or under `src/collections/`, treat it as a desktop brief rather than a live collector implementation.

| Collection | Platform | Source | Page | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| `linux_shell_history_collection` | Linux | `src/collection_catalog.rs` | [linux shell history collection](../parsers/linux_shell_history.md) | Catalog only | Bound to `linux_shell_history` |
| `macos_browser_history_collection` | macOS | `src/collection_catalog.rs` | [macOS browser history collection](../parsers/macos_browser_history.md) | Catalog only | Bound to `macos_browser_history` |
| `macos_quarantine_events_collection` | macOS | `src/collection_catalog.rs` | [macOS quarantine events collection](../parsers/macos_quarantine_events.md) | Catalog only | Bound to `macos_quarantine_events` |
| `windows_browser_artifacts_collection` | Windows | `src/collections/windows/browser_artifacts.rs` | [Windows Browser Artifacts collection](windows/browser_artifacts.md) | VSS snapshot targeted browser artifact copy with desktop zip packaging | Same binary subcommand: `holo-forensics collect-browser-artifacts`; copies targeted Chrome/Edge databases, sessions, storage, extension manifests, Firefox, Edge Legacy/WebCache, DPAPI, NTUSER, and system hive support material from a VSS snapshot; bound to `windows_browser_history`; shares one VSS snapshot with Registry/USN/EVTX/SRUM/Prefetch/Jump Lists/LNK Files/MFT/LogFile/INDX in combined archive collection |
| `windows_prefetch_collection` | Windows | `src/collections/windows/prefetch.rs` | [Windows Prefetch collection](windows/prefetch.md) | VSS snapshot Prefetch copy with desktop zip packaging | Same binary subcommand: `holo-forensics collect-prefetch`; copies `*.pf`, `Layout.ini`, and `Ag*.db` from the Prefetch directory in a VSS snapshot, streams bytes while hashing, records timestamps and file attributes, and shares one VSS snapshot with Registry/USN/EVTX/SRUM/Browser Artifacts/Jump Lists/LNK Files/MFT/LogFile/INDX in combined archive collection; bound to `windows_prefetch` |
| `windows_mplogs_collection` | Windows | `src/collections/windows/mplogs.rs` | [Windows Microsoft Protection Logs collection](windows/mplogs.md) | VSS snapshot Defender Support MPLog copy with desktop zip packaging | Same binary subcommand: `holo-forensics collect-mplogs`; copies `ProgramData\Microsoft\Windows Defender\Support\MPLog*.log` from a VSS snapshot, hashes source and destination bytes, skips registry and EVTX duplication already covered elsewhere, shares one VSS snapshot with Registry/USN/EVTX/SRUM/Prefetch/Scheduled Tasks/WMI Repository/PowerShell Activity/Browser Artifacts/Jump Lists/LNK Files/Recycle Bin/MFT/LogFile/INDX in combined archive collection, and feeds `windows_mplogs` |
| `windows_bits_collection` | Windows | `src/collection_catalog.rs` | [Windows BITS collection](windows/bits.md) | Catalog only raw-input parser contract | Bound to `windows_bits`; accepts `qmgr.db`, `qmgr0.dat`, and `qmgr1.dat` from supplied evidence packages; no Create Package collector yet |
| `windows_search_collection` | Windows | `src/collection_catalog.rs` | [Windows Search collection](windows/search.md) | Catalog only raw-input parser contract | Bound to `windows_search`; accepts `Windows.edb` and `Windows.db` from supplied evidence packages; no Create Package collector yet |
| `windows_outlook_collection` | Windows | `src/collection_catalog.rs` | [Windows Outlook collection](windows/outlook.md) | Catalog only raw-input parser contract | Bound to `windows_outlook`; accepts `.ost` and `.pst` stores from supplied evidence packages; no Create Package collector yet |
| `windows_shimdb_collection` | Windows | `src/collection_catalog.rs` | [Windows Shim Database collection](windows/shimdb.md) | Catalog only raw-input parser contract | Bound to `windows_shimdb`; accepts `.sdb` files from supplied evidence packages; no Create Package collector yet |
| `windows_evtx_collection` | Windows | `src/collections/windows/evtx.rs` | [Windows EVTX collection](windows/evtx.md) | VSS snapshot EVTX copy with desktop zip packaging | Same binary subcommand: `holo-forensics collect-evtx`; physical `.evtx` copy from VSS snapshot; shares one VSS snapshot with Registry/USN/MFT/LogFile/INDX/SRUM/Prefetch/Browser Artifacts/Jump Lists/LNK Files in combined archive collection; bound to `windows_event_logs` |
| `windows_scheduled_tasks_collection` | Windows | `src/collections/windows/scheduled_tasks.rs` | [Windows Scheduled Tasks collection](windows/scheduled_tasks.md) | VSS snapshot raw scheduled task copy with desktop zip packaging | Same binary subcommand: `holo-forensics collect-scheduled-tasks`; copies legacy `Windows\Tasks`, `Windows\SchedLgU.txt`, and modern `Windows\System32\Tasks` from a VSS snapshot, skips reparse points, hashes source and destination bytes, records directory metadata, and shares one VSS snapshot with Registry/USN/EVTX/SRUM/Prefetch/Browser Artifacts/Jump Lists/LNK Files/Recycle Bin/MFT/LogFile/INDX in combined archive collection; bound to `windows_scheduled_tasks` |
| `windows_wmi_repository_collection` | Windows | `src/collections/windows/wmi_repository.rs` | [Windows WMI Repository collection](windows/wmi_repository.md) | VSS snapshot raw WMI repository copy with desktop zip packaging | Same binary subcommand: `holo-forensics collect-wmi-repository`; copies `Windows\System32\wbem\Repository*\**`, `Windows\System32\wbem\AutoRecover\**`, and top-level `Windows\System32\wbem\*.mof` / `*.mfl` from a VSS snapshot, preserves raw repository bytes with directory metadata, skips registry and EVTX duplication already covered elsewhere, and shares one VSS snapshot with Registry/USN/EVTX/SRUM/Prefetch/Scheduled Tasks/PowerShell Activity/Browser Artifacts/Jump Lists/LNK Files/Recycle Bin/MFT/LogFile/INDX in combined archive collection; bound to `windows_wmi_persistence` |
| `windows_powershell_activity_collection` | Windows | `src/collections/windows/powershell_activity.rs` | [Windows PowerShell Activity collection](windows/powershell_activity.md) | VSS snapshot user PowerShell artifact copy with desktop zip packaging | Same binary subcommand: `holo-forensics collect-powershell-activity`; enumerates eligible user profiles under `Users`, copies PSReadLine history and profile scripts, selectively copies transcript/module/config files from user PowerShell roots under a size limit, records skipped files with size and reason, skips registry and EVTX duplication, and shares one VSS snapshot with Registry/USN/EVTX/SRUM/Prefetch/Scheduled Tasks/Browser Artifacts/Jump Lists/LNK Files/Recycle Bin/MFT/LogFile/INDX in combined archive collection |
| `windows_lnk_collection` | Windows | `src/collections/windows/lnk.rs` | [Windows LNK collection](windows/lnk.md) | VSS snapshot raw LNK copy with JSONL artifact manifest and desktop zip packaging | Same binary subcommand: `holo-forensics collect-lnk`; copies raw `.lnk` files from per-user Recent, Office Recent, Desktop, user Start Menu, and ProgramData Start Menu roots in a VSS snapshot, does not resolve shortcut targets, writes `C/lnk_manifest.jsonl`, and shares one VSS snapshot with Registry/USN/EVTX/SRUM/Prefetch/Browser Artifacts/Jump Lists/MFT/LogFile/INDX in combined archive collection; bound to `windows_shortcuts` |
| `windows_jump_lists_collection` | Windows | `src/collections/windows/jump_lists.rs` | [Windows Jump Lists collection](windows/jump_lists.md) | VSS snapshot Jump List copy with JSONL artifact manifest and desktop zip packaging | Same binary subcommand: `holo-forensics collect-jump-lists`; copies per-user AutomaticDestinations and CustomDestinations from a VSS snapshot, preserves original Windows paths, hashes each file, writes `C/jump_lists_manifest.jsonl`, and shares one VSS snapshot with Registry/USN/EVTX/SRUM/Browser Artifacts/LNK Files/MFT/LogFile/INDX in combined archive collection; bound to `windows_jump_lists` |
| `windows_recycle_bin_info2_collection` | Windows | `src/collections/windows/recycle_bin.rs` | [Windows Recycle Bin collection](windows/recycle_bin.md) | VSS snapshot raw Recycle Bin copy with JSONL artifact manifest and desktop zip packaging | Same binary subcommand: `holo-forensics collect-recycle-bin`; copies both modern `$Recycle.Bin` and legacy `Recycler` roots from a VSS snapshot without restoring original names, preserves root-level files and nested `$R` directories, hashes each copied file, writes `C/recycle_bin_manifest.jsonl`, feeds `windows_recycle_bin` for modern `$I*` metadata and `windows_recycle_bin_info2` for XP `INFO2`, and shares one VSS snapshot with Registry/USN/EVTX/SRUM/Prefetch/Browser Artifacts/Jump Lists/LNK Files/MFT/LogFile/INDX in combined archive collection |
| `windows_indx_collection` | Windows | `src/collections/windows/indx.rs` | [Windows INDX collection](windows/indx.md) | VSS raw NTFS + explicit raw live mode with desktop zip packaging | Same binary subcommand: `holo-forensics collect-indx`; walks directory MFT records, preserves `$INDEX_ROOT:$I30`, `$INDEX_ALLOCATION:$I30`, and `$BITMAP:$I30` into `C/INDX.rawpack`, hashes entries and the pack, and shares one VSS snapshot with Registry/USN/EVTX/MFT/LogFile/SRUM/Prefetch/Browser Artifacts/Jump Lists/LNK Files in combined archive collection |
| `windows_logfile_collection` | Windows | `src/collections/windows/logfile.rs` | [Windows LogFile collection](windows/logfile.md) | VSS raw NTFS + explicit raw live mode with desktop zip packaging | Same binary subcommand: `holo-forensics collect-logfile`; resolves MFT record 2, extracts `$LogFile` from its unnamed `$DATA` stream, hashes output, and shares one VSS snapshot with Registry/USN/EVTX/MFT/INDX/SRUM/Prefetch/Browser Artifacts/Jump Lists/LNK Files in combined archive collection |
| `windows_mft_collection` | Windows | `src/collections/windows/mft.rs` | [Windows MFT collection](windows/mft.md) | VSS raw NTFS + explicit raw live mode with desktop zip packaging | Same binary subcommand: `holo-forensics collect-mft`; opens the VSS snapshot or live volume as a raw NTFS device, extracts `$MFT` from its unnamed `$DATA` stream, hashes output, and shares one VSS snapshot with Registry/USN/EVTX/LogFile/INDX/SRUM/Prefetch/Browser Artifacts/Jump Lists/LNK Files in combined archive collection; bound to `windows_mft` |
| `windows_registry_collection` | Windows | `src/collections/windows/registry.rs` | [Windows registry collection](windows/registry.md) | VSS snapshot hive + log collection with desktop zip packaging | Same binary subcommand: `holo-forensics collect-registry`; bound to `windows_registry` plus `windows_userassist`, `windows_shimcache`, `windows_shellbags`, `windows_amcache`, `windows_users`, and `windows_services`; `RegSaveKeyExW` remains available as an explicit fallback mode; shares one VSS snapshot with USN/EVTX/MFT/LogFile/INDX/SRUM/Prefetch/Browser Artifacts/Jump Lists/LNK Files in combined archive collection |
| `windows_srum_collection` | Windows | `src/collections/windows/srum.rs` | [Windows SRUM collection](windows/srum.md) | VSS snapshot SRU folder copy with desktop zip packaging | Same binary subcommand: `holo-forensics collect-srum`; copies `Windows\System32\sru\*` plus supporting `SOFTWARE` and `SYSTEM` hives from a VSS snapshot; shares one VSS snapshot with Registry/USN/EVTX/MFT/LogFile/INDX/Prefetch/Browser Artifacts/Jump Lists/LNK Files in combined archive collection; bound to `windows_srum` |
| `windows_restore_point_log_collection` | Windows | `src/collection_catalog.rs` | [Windows restore point log collection](../parsers/windows/restore_point_log.md) | Catalog only | Bound to `windows_restore_point_log` |
| `windows_timeline_collection` | Windows | `src/collection_catalog.rs` | [Windows Timeline collection](../parsers/windows/windows_timeline.md) | Catalog only | Bound to `windows_timeline` |
| `windows_usn_journal_collection` | Windows | `src/collections/windows/usn_journal.rs` | [windows_usn_journal_collection](windows/usn_journal.md) | Direct-stream + VSS raw NTFS + sparse logical output + desktop zip packaging | Same binary subcommand: `holo-forensics collect-usn-journal`; bound to `windows_usn_journal`; shares one VSS snapshot with Registry/EVTX/MFT/LogFile/INDX/SRUM/Prefetch/Browser Artifacts/Jump Lists/LNK Files in combined archive collection |

## Required Updates

- Keep this index aligned with `src/collection_catalog.rs`.
- Add a new page here whenever a new collection contract or collector implementation is enabled.
- Update `Status` when the collector implementation mode changes.
- Update the affected collection page whenever a major collector behavior, acquisition method, output layout, manifest schema, or validation status changes.
- New live collectors that write per-collector metadata into a collection zip must use the centralized archive layout from `src/collection_metadata.rs`: `$metadata/collectors/<volume>/<collector>/manifest.json`. If the collector writes an operator-facing collection log, place it beside the manifest as `collection.log`.
