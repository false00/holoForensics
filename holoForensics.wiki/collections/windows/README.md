# Windows Collections

This section tracks the Windows collection contracts in `src/collection_catalog.rs` and the live Windows collectors under `src/collections/windows/`.

The desktop Collection tab reflects the live collection surfaces listed below. Registry-derived artifacts such as `Amcache.hve`, ShimCache/AppCompatCache, UserAssist, Shellbags, local users, and Services remain part of the Registry Hives collector rather than standalone collection cards. Catalog-only raw-input contracts for BITS, Windows Search, Outlook stores, Shim databases, restore-point logs, and Timeline are parser-facing only and do not yet have Create Package collectors.

| Collection | Source | Page | Status | Notes |
| --- | --- | --- | --- | --- |
| `windows_browser_artifacts_collection` | `src/collections/windows/browser_artifacts.rs` | [browser_artifacts](browser_artifacts.md) | VSS snapshot targeted browser artifact copy with desktop zip packaging | Same binary subcommand: `holo-forensics collect-browser-artifacts`; copies targeted Chrome/Edge databases, sessions, storage, extension manifests, Firefox, Edge Legacy/WebCache, DPAPI, NTUSER, and system hive support material from a VSS snapshot and writes centralized metadata |
| `windows_prefetch_collection` | `src/collections/windows/prefetch.rs` | [prefetch](prefetch.md) | VSS snapshot Prefetch copy with desktop zip packaging | Same binary subcommand: `holo-forensics collect-prefetch`; copies `*.pf`, `Layout.ini`, and `Ag*.db` from the Prefetch directory in a VSS snapshot, streams bytes while hashing, records timestamps and file attributes, writes centralized metadata, and feeds `windows_prefetch` |
| `windows_bits_collection` | `src/collection_catalog.rs` | [bits](bits.md) | Catalog only raw-input parser contract | Bound to `windows_bits`; accepts `qmgr.db`, `qmgr0.dat`, and `qmgr1.dat` from supplied evidence packages; no live collector |
| `windows_search_collection` | `src/collection_catalog.rs` | [search](search.md) | Catalog only raw-input parser contract | Bound to `windows_search`; accepts `Windows.edb` and `Windows.db` from supplied evidence packages; no live collector |
| `windows_outlook_collection` | `src/collection_catalog.rs` | [outlook](outlook.md) | Catalog only raw-input parser contract | Bound to `windows_outlook`; accepts `.ost` and `.pst` stores from supplied evidence packages; no live collector |
| `windows_shimdb_collection` | `src/collection_catalog.rs` | [shimdb](shimdb.md) | Catalog only raw-input parser contract | Bound to `windows_shimdb`; accepts `.sdb` files from supplied evidence packages; no live collector |
| `windows_powershell_activity_collection` | `src/collections/windows/powershell_activity.rs` | [powershell_activity](powershell_activity.md) | VSS snapshot user PowerShell artifact copy with desktop zip packaging | Same binary subcommand: `holo-forensics collect-powershell-activity`; copies PSReadLine history, user profile scripts, likely transcripts, and selected script/config files from user PowerShell roots in a VSS snapshot, records skipped files with size and reason, and writes centralized metadata |
| `windows_scheduled_tasks_collection` | `src/collections/windows/scheduled_tasks.rs` | [scheduled_tasks](scheduled_tasks.md) | VSS snapshot raw scheduled task copy with desktop zip packaging | Same binary subcommand: `holo-forensics collect-scheduled-tasks`; copies legacy `Windows\Tasks`, `Windows\SchedLgU.txt`, and modern `Windows\System32\Tasks` from a VSS snapshot, skips reparse points, hashes source and destination bytes, records directory metadata, writes centralized metadata, and feeds `windows_scheduled_tasks` |
| `windows_wmi_repository_collection` | `src/collections/windows/wmi_repository.rs` | [wmi_repository](wmi_repository.md) | VSS snapshot raw WMI repository copy with desktop zip packaging | Same binary subcommand: `holo-forensics collect-wmi-repository`; copies `Windows\System32\wbem\Repository*\**`, `Windows\System32\wbem\AutoRecover\**`, and top-level `Windows\System32\wbem\*.mof` / `*.mfl` from a VSS snapshot, preserves raw repository bytes with directory metadata, and feeds `windows_wmi_persistence` |
| `windows_jump_lists_collection` | `src/collections/windows/jump_lists.rs` | [jump_lists](jump_lists.md) | VSS snapshot Jump List copy with JSONL artifact manifest and desktop zip packaging | Same binary subcommand: `holo-forensics collect-jump-lists`; copies per-user AutomaticDestinations and CustomDestinations from a VSS snapshot, hashes each file, writes `C/jump_lists_manifest.jsonl`, centralizes collector metadata, and feeds `windows_jump_lists` |
| `windows_lnk_collection` | `src/collections/windows/lnk.rs` | [lnk](lnk.md) | VSS snapshot raw LNK copy with JSONL artifact manifest and desktop zip packaging | Same binary subcommand: `holo-forensics collect-lnk`; copies raw `.lnk` files from per-user Recent, Office Recent, Desktop, user Start Menu, and ProgramData Start Menu roots in a VSS snapshot, does not resolve shortcut targets, hashes each file, writes `C/lnk_manifest.jsonl`, centralizes collector metadata, and feeds `windows_shortcuts` |
| `windows_evtx_collection` | `src/collections/windows/evtx.rs` | [evtx](evtx.md) | VSS snapshot EVTX copy with desktop zip packaging | Same binary subcommand: `holo-forensics collect-evtx`; copies all `*.evtx` under `Windows\System32\winevt\Logs` from a VSS snapshot, including archived logs, and feeds `windows_event_logs` |
| `windows_registry_collection` | `src/collections/windows/registry.rs` | [registry](registry.md) | VSS snapshot hive + log collection with desktop zip packaging | Same binary subcommand: `holo-forensics collect-registry`; feeds `windows_registry` plus `windows_userassist`, `windows_shimcache`, `windows_shellbags`, `windows_amcache`, `windows_users`, and `windows_services`; `RegSaveKeyExW` remains an explicit fallback mode |
| `windows_mft_collection` | `src/collections/windows/mft.rs` | [mft](mft.md) | VSS raw NTFS + explicit raw live mode with desktop zip packaging | Same binary subcommand: `holo-forensics collect-mft`; default mode is VSS, raw live mode must be requested explicitly with `--mode raw`, and the contract feeds `windows_mft` |
| `windows_logfile_collection` | `src/collections/windows/logfile.rs` | [logfile](logfile.md) | VSS raw NTFS + explicit raw live mode with desktop zip packaging | Same binary subcommand: `holo-forensics collect-logfile`; default mode is VSS, raw live mode must be requested explicitly with `--mode raw`; no built-in parser family is currently bound |
| `windows_indx_collection` | `src/collections/windows/indx.rs` | [indx](indx.md) | VSS raw NTFS + explicit raw live mode with desktop zip packaging | Same binary subcommand: `holo-forensics collect-indx`; walks NTFS directory records and packs `$INDEX_ROOT:$I30`, `$INDEX_ALLOCATION:$I30`, and `$BITMAP:$I30` into `C/INDX.rawpack`; no built-in parser family is currently bound |
| `windows_srum_collection` | `src/collections/windows/srum.rs` | [srum](srum.md) | VSS snapshot SRU folder copy with desktop zip packaging | Same binary subcommand: `holo-forensics collect-srum`; copies the SRU folder plus `SOFTWARE` and `SYSTEM` from a VSS snapshot, writes centralized metadata, and feeds `windows_srum` |
| `windows_restore_point_log_collection` | `src/collection_catalog.rs` | [restore_point_log](../../parsers/windows/restore_point_log.md) | Catalog only | Bound to `windows_restore_point_log` |
| `windows_recycle_bin_info2_collection` | `src/collections/windows/recycle_bin.rs` | [recycle_bin](recycle_bin.md) | VSS snapshot raw Recycle Bin copy with JSONL artifact manifest and desktop zip packaging | Same binary subcommand: `holo-forensics collect-recycle-bin`; copies both modern `$Recycle.Bin` and legacy `Recycler` roots from a VSS snapshot, centralizes metadata, and feeds both `windows_recycle_bin` and `windows_recycle_bin_info2` |
| `windows_timeline_collection` | `src/collection_catalog.rs` | [windows_timeline](../../parsers/windows/windows_timeline.md) | Catalog only | Bound to `windows_timeline` |
| `windows_usn_journal_collection` | `src/collections/windows/usn_journal.rs` | [usn_journal](usn_journal.md) | Direct-stream + VSS raw NTFS + sparse logical output + desktop zip packaging | Same binary subcommand: `holo-forensics collect-usn-journal`; bound to `windows_usn_journal`; shares one VSS snapshot with Registry/EVTX/MFT/LogFile/INDX/SRUM/Browser Artifacts/Jump Lists/LNK Files in combined archive collection |

## Required Updates

- Keep this index aligned with `src/collection_catalog.rs` and `src/collections/windows/`.
- Update `Status` when allocated-range or raw-volume fallback modes land.
- Update the affected Windows collection page whenever a major collector behavior, acquisition method, output layout, manifest schema, or validation status changes.
- Windows collection zips centralize collector metadata under `$metadata/collectors/<volume>/<collector>/`. Use `manifest.json` for each collector manifest and `collection.log` for an optional collector log. Do not add new collector manifests next to the collected artifact payload unless the collector also needs a standalone CLI sidecar for backwards compatibility.
