# Project Guidelines

Canonical assistant-facing repository instructions live in `AGENTS.md` at the workspace root.

- Keep assistant-only workflow and repository policy there instead of duplicating it in this file.
- Use `AGENTS.md` as the source of truth for architecture, documentation contract, and commit or push guidance.
